# Liveart Image Editor

A small browser-based photo editor: image upload, cropping, non-destructive adjustments (brightness/contrast/saturation), filters (grayscale/sepia), result export, and JSON export describing the applied operations.

Stack: Vue 3, Vuetify 3, Pinia, TypeScript, Vite.

## Run

```bash
npm i
npm run dev
```
